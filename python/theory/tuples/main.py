#definicion: las tuplas son listas constantes, un vez declarados solo estan disponibles en modo lectura

tupla = ('str',1,2.5,True,[1,8,8],('asd','asda'))

#tuple(lista)        crea una tupla en base a una lista
#list(tupla)         crea una lista en base a una tupla