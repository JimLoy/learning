'''
str.split(value1,value2)        retorna una lista a partir de un str (por defecto el caracter del v1 es ' ') v2 determina las veces que se separara el str
' '.join(list)                  retorna un str a partir de una lista con la ceparacion del inicio (es muy comun el ' ')      
str.count(value)                retorna el numero de coinsidencias que tiene el valor en el str
str1 in str2                    retorna un booleano si str1 existe dentro de str2
str.lower()                     str a minuscula
str.uper()                      str a mayuscula
str.startswith(value)           devuelve true si str empieza con value
str.ljust(20)                   retorna un str con 20 espacios a la derecha
str.rjust(20)                   retorna un str con 20 espacios a la izquierda
str.center(20)                  retorna un str con 10 espacios a la izquierda y a la derecha

'''

